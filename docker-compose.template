version: "3.8"
services:
  app:
    build: .
    logging:
      driver: syslog
    tmpfs:
    - /tmp
    hostname: snapshot-processor
    ports:
      - "21:21"
    volumes:
      - ./data:/data
      - /dev/log:/dev/log
    environment:
      FLASK_ENV: development
